import React, {Component} from 'react';
import {Text, View} from 'react-native';

export default class Cliente extends Component {
  render() {
    return (
      <View>
        <Text> dale Cliente </Text>
      </View>
    );
  }
}
